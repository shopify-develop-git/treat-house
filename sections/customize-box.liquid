{%- comment -%}
  The box builder: four steps that end in one line item.

  What the store already knows, and what this therefore does not decide:

  The three pack sizes are three products, not one product with a size option, so
  step 1 chooses a product. Packaging is that product's own two options — a gift
  box, individually wrapped — so step 3 chooses a variant of it. Between them
  every combination is a variant Shopify has already priced, which is why nothing
  here adds money up: each packaging block publishes the price of the variant it
  resolves to for every pack, and the summary shows whichever one is currently
  chosen. A builder that totalled its own prices would eventually disagree with
  the cart, and the cart would be right.

  Flavours and the gift message are not variants — they are what the box holds
  and what is written on the card — so they travel as line item properties.

  The result is one `/cart/add`, no app, and a price that is Shopify's throughout.
{%- endcomment -%}

{%- liquid
  assign packs = section.settings.pack_collection.products
  # The ten treats the file draws exist as products but belong to no collection,
  # so a picked list is the shorter road: a shop that keeps its flavours in a
  # collection can still point at one, and the list wins when both are set.
  assign flavours = section.settings.flavour_products
  if flavours == blank or flavours.size == 0
    assign flavours = section.settings.flavour_collection.products
  endif
  assign first_pack = packs.first

  assign scheme_class = 'color-' | append: section.settings.color_scheme

  # `render` takes no filters on a named argument, so every translated label is
  # resolved here rather than at the call.
  assign back_label = 'actions.back' | t
  assign continue_label = 'actions.continue' | t
  assign add_to_cart_label = 'actions.add_to_cart' | t
  assign full_heading = section.settings.heading | append: ' ' | append: section.settings.heading_accent | strip
  assign breadcrumb_current = page.title | default: full_heading
-%}

<div
  class="section section--{{ section.settings.section_width }} spacing-style {{ scheme_class }} customize-box"
  style="{% render 'spacing-style', settings: section.settings %}"
  {{ section.shopify_attributes }}
>
  {% render 'ui-breadcrumb',
    parent_title: section.settings.breadcrumb_parent_title,
    parent_url: section.settings.breadcrumb_parent_url,
    current: breadcrumb_current
  %}

  {%- comment -%}
    The band is full width inside the page column, not full bleed: the file runs
    it from margin to margin like every card under it.
  {%- endcomment -%}
  <div class="customize-box__hero">
    {%- if section.settings.eyebrow != blank -%}
      <p class="customize-box__eyebrow">
        <span class="customize-box__eyebrow-icon" aria-hidden="true">
          {{- 'icon-ui-square-pen.svg' | inline_asset_content -}}
        </span>
        {{ section.settings.eyebrow }}
      </p>
    {%- endif -%}

    {%- comment -%}
      Past the first step the file swaps the title — "Choose Your Own Flavors"
      becomes "Customize Your Box" (508:9503 against 508:9631). Both wordings ride
      on the spans as attributes rather than living in the script, so the merchant
      still owns the copy and a translation still travels with it.

      Both spans always render, even when a wording is blank: the script fills
      them, and a span that was never drawn cannot be filled later.
    {%- endcomment -%}
    <h1 class="customize-box__heading">
      <span
        data-heading-part
        data-first="{{ section.settings.heading | escape }}"
        data-after="{{ section.settings.heading_after | escape }}"
      >{{ section.settings.heading }}</span>
      <span
        class="customize-box__heading-accent"
        data-heading-part
        data-first="{{ section.settings.heading_accent | escape }}"
        data-after="{{ section.settings.heading_accent_after | escape }}"
      >{{ section.settings.heading_accent }}</span>
    </h1>

    {%- if section.settings.subheading != blank -%}
      <p class="customize-box__subheading">{{ section.settings.subheading }}</p>
    {%- endif -%}
  </div>

  {%- if first_pack == blank -%}
    <p class="customize-box__setup">
      {{ 'content.customize_box_setup' | t }}
    </p>
  {%- else -%}
    <customize-box
      class="customize-box__builder"
      data-total-steps="4"
    >
      {%- comment -%}
        No product form here, and no pack variant. A box is a set of lines now —
        one per flavour, one for the packaging — which `{% form 'product' %}` and
        Horizon's product-form-component cannot express: both are built around a
        single `id`. The script posts the set to /cart/add.js itself and then
        announces it with the theme's own CartAddEvent, so the drawer and the
        count still refresh the way they do everywhere else.
      {%- endcomment -%}
      <div class="customize-box__form">
        <div
          class="visually-hidden"
          aria-live="assertive"
          role="status"
          aria-atomic="true"
          data-live-region
        ></div>

          <div class="customize-box__layout">
            <div class="customize-box__main">
              {%- comment -%} Screen 1 carries steps 1 and 2, as the file draws them. {%- endcomment -%}
              <section class="customize-box__screen" data-screen="1">
                <div class="customize-box__step">
                  {% render 'ui-step-progress', step: 1, total: 4 %}
                  <div class="customize-box__card">
                    <div class="customize-box__card-head">
                      <h2 class="customize-box__card-title">
                        <span class="customize-box__card-number">1.</span>
                        {{ section.settings.step_1_title }}
                      </h2>
                      {%- if section.settings.step_1_intro != blank -%}
                        <p class="customize-box__card-intro">{{ section.settings.step_1_intro }}</p>
                      {%- endif -%}
                    </div>
                    <hr class="customize-box__rule">

                    <div class="customize-box__packs" role="radiogroup" aria-label="{{ section.settings.step_1_title | escape }}">
                      {%- for pack in packs -%}
                        {%- liquid
                          assign short_title = pack.title | split: '–' | first | strip
                          assign pack_size = pack.metafields.custom.pack_size.value | default: pack.metafields.custom.pack_size
                          if pack_size == blank
                            assign pack_size = pack.title | split: ' ' | first | times: 1
                          endif

                          assign base_variant = pack.variants.first
                          for variant in pack.variants
                            if variant.price < base_variant.price
                              assign base_variant = variant
                            endif
                          endfor

                          assign per_treat = ''
                          if pack_size > 0
                            assign per_unit = base_variant.price | divided_by: pack_size
                            assign per_treat = per_unit | money | append: ' / ' | append: section.settings.unit_noun
                          endif

                          # Escaped into their own variables first. A filter in the
                          # middle of an append chain applies to everything
                          # accumulated so far, not to the value beside it.
                          assign safe_title = short_title | escape
                          assign safe_note = per_treat | escape
                          assign tile_attributes = 'data-pack="' | append: pack.handle
                          assign tile_attributes = tile_attributes | append: '" data-pack-size="' | append: pack_size
                          assign tile_attributes = tile_attributes | append: '" data-pack-title="' | append: safe_title
                          assign tile_attributes = tile_attributes | append: '" data-pack-note="' | append: safe_note | append: '"'
                        -%}
                        {% render 'ui-choice-tile',
                          name: 'customize-pack',
                          value: pack.handle,
                          title: short_title,
                          note: per_treat,
                          image: pack.featured_image,
                          attributes: tile_attributes
                        %}
                      {%- endfor -%}
                    </div>
                  </div>
                </div>

                <div class="customize-box__step">
                  {% render 'ui-step-progress', step: 2, total: 4 %}
                  <div class="customize-box__card">
                    <div class="customize-box__card-head">
                      <h2 class="customize-box__card-title">
                        <span class="customize-box__card-number">2.</span>
                        {{ section.settings.step_2_title }}
                      </h2>
                      {%- if section.settings.step_2_intro != blank -%}
                        <p class="customize-box__card-intro">{{ section.settings.step_2_intro }}</p>
                      {%- endif -%}
                    </div>
                    <hr class="customize-box__rule">

                    {%- if flavours.size > 0 -%}
                      <div class="customize-box__flavours">
                        {%- for flavour in flavours -%}
                          {%- capture flavour_season -%}{% render 'season-status', product: flavour %}{%- endcapture -%}
                          {%- liquid
                            # A seasonal flavour that is closed or not yet open is left out of
                            # the builder rather than drawn disabled — see [season-status].
                            assign flavour_season_status = flavour_season | strip | split: '|' | first
                            if flavour_season_status != blank and flavour_season_status != 'available'
                              continue
                            endif

                            assign flavour_variant = flavour.first_available_variant | default: flavour.variants.first
                            assign flavour_attributes = 'data-flavour-variant="' | append: flavour_variant.id
                            assign flavour_attributes = flavour_attributes | append: '" data-flavour-available="' | append: flavour_variant.available | append: '"'
                          -%}
                          {% render 'ui-flavour-card',
                            handle: flavour.handle,
                            title: flavour.title,
                            image: flavour.featured_image,
                            attributes: flavour_attributes
                          %}
                        {%- endfor -%}
                      </div>
                      <p class="customize-box__tally" data-tally aria-live="polite"></p>
                    {%- else -%}
                      <p class="customize-box__setup">{{ 'content.customize_box_flavours_setup' | t }}</p>
                    {%- endif -%}

                    <hr class="customize-box__rule">
                    <div class="customize-box__actions">
                      {% render 'ui-button', variant: 'secondary', label: back_label, attributes: 'data-step-back' %}
                      {% render 'ui-button', variant: 'primary', label: continue_label, attributes: 'data-step-next' %}
                    </div>
                  </div>
                </div>
              </section>

              <section class="customize-box__screen" data-screen="2" hidden>
                {% render 'ui-step-progress', step: 3, total: 4 %}
                <div class="customize-box__card">
                  <div class="customize-box__card-head">
                    <h2 class="customize-box__card-title">
                      <span class="customize-box__card-number">3.</span>
                      {{ section.settings.step_3_title }}
                    </h2>
                    {%- if section.settings.step_3_intro != blank -%}
                      <p class="customize-box__card-intro">{{ section.settings.step_3_intro }}</p>
                    {%- endif -%}
                  </div>
                  <hr class="customize-box__rule">

                  <div class="customize-box__packaging" role="radiogroup" aria-label="{{ section.settings.step_3_title | escape }}">
                    {% content_for 'blocks' %}
                  </div>

                  <hr class="customize-box__rule">
                  <div class="customize-box__actions">
                    {% render 'ui-button', variant: 'secondary', label: back_label, attributes: 'data-step-back' %}
                    {% render 'ui-button', variant: 'primary', label: continue_label, attributes: 'data-step-next' %}
                  </div>
                </div>
              </section>

              <section class="customize-box__screen" data-screen="3" hidden>
                {% render 'ui-step-progress', step: 4, total: 4 %}
                <div class="customize-box__card">
                  <div class="customize-box__card-head">
                    <h2 class="customize-box__card-title">
                      <span class="customize-box__card-number">4.</span>
                      {{ section.settings.step_4_title }}
                    </h2>
                    {%- if section.settings.step_4_intro != blank -%}
                      <p class="customize-box__card-intro">{{ section.settings.step_4_intro }}</p>
                    {%- endif -%}
                  </div>
                  <hr class="customize-box__rule">

                  {% render 'ui-message',
                    placeholder: section.settings.message_placeholder,
                    maxlength: section.settings.message_maxlength,
                    class: 'customize-box__message',
                    attributes: 'data-message'
                  %}

                  <hr class="customize-box__rule">
                  <div class="customize-box__actions">
                    {% render 'ui-button', variant: 'secondary', label: back_label, attributes: 'data-step-back' %}
                    {% render 'ui-button', variant: 'primary', label: continue_label, attributes: 'data-step-next' %}
                  </div>
                </div>
              </section>

              <section class="customize-box__screen" data-screen="4" hidden>
                <div class="customize-box__card">
                  <div class="customize-box__card-head">
                    <h2 class="customize-box__card-title">{{ section.settings.review_title }}</h2>
                    {%- if section.settings.review_intro != blank -%}
                      <p class="customize-box__card-intro">{{ section.settings.review_intro }}</p>
                    {%- endif -%}
                  </div>
                  <hr class="customize-box__rule">

                  <div class="customize-box__review">
                    {% render 'ui-review-row', label: section.settings.label_flavours, value: '', edit_step: '1', class: 'customize-box__review-flavours' %}
                    {% render 'ui-review-row', label: section.settings.label_quantity, value: '', edit_step: '1', class: 'customize-box__review-pack' %}
                    {% render 'ui-review-row', label: section.settings.label_packaging, value: '', edit_step: '2', class: 'customize-box__review-packaging' %}
                    {% render 'ui-review-row', label: section.settings.label_message, value: '', edit_step: '3', class: 'customize-box__review-message' %}
                  </div>

                  <hr class="customize-box__rule">
                  <div class="customize-box__actions">
                    {% render 'ui-button', variant: 'secondary', label: back_label, attributes: 'data-step-back' %}
                  </div>
                </div>
              </section>

              {%- if section.settings.promo_title != blank -%}
                <aside class="customize-box__promo">
                  {%- comment -%}
                    The title is a child of the strip, not of the copy beside the
                    button: the file gives it a row of its own across the full
                    width, and a grid can only span something it owns.
                  {%- endcomment -%}
                  <p class="customize-box__promo-title">
                    <span class="customize-box__promo-icon" aria-hidden="true">
                      {{- 'icon-ui-tag.svg' | inline_asset_content -}}
                    </span>
                    {{ section.settings.promo_title }}
                  </p>
                  {%- if section.settings.promo_text != blank -%}
                    <div class="customize-box__promo-text">
                      <p class="customize-box__promo-copy">{{ section.settings.promo_text }}</p>
                    </div>
                  {%- endif -%}
                  {%- if section.settings.promo_button_label != blank -%}
                    {% render 'ui-button',
                      variant: 'secondary',
                      label: section.settings.promo_button_label,
                      link: section.settings.promo_button_link
                    %}
                  {%- endif -%}
                </aside>
              {%- endif -%}
            </div>

            <aside class="customize-box__summary">
              <div class="customize-box__summary-inner">
                <h2 class="customize-box__summary-title">{{ section.settings.summary_title }}</h2>
                <hr class="customize-box__rule">

                <div class="customize-box__summary-row">
                  <span>{{ section.settings.label_quantity }}</span>
                  <strong data-summary-pack>&mdash;</strong>
                </div>

                <div class="customize-box__summary-row">
                  <span>{{ section.settings.label_flavours_selected }}</span>
                  <strong data-summary-flavour-count>0</strong>
                </div>

                <div class="customize-box__summary-chips" data-summary-chips hidden></div>

                {%- comment -%}
                  The name and the surcharge are two spans rather than one string
                  because the file sets them differently — the choice in bold at
                  16, the "(+ $6.00)" beside it regular at 14. The two carry no
                  whitespace between them in the source; the space belongs to the
                  surcharge, so a choice that adds nothing leaves no gap behind.
                {%- endcomment -%}
                <div class="customize-box__summary-row" data-summary-packaging-row hidden>
                  <span>{{ section.settings.label_packaging }}</span>
                  <strong data-summary-packaging><span data-summary-packaging-name></span><span class="customize-box__summary-extra" data-summary-packaging-extra></span></strong>
                </div>

                <div class="customize-box__summary-row" data-summary-message-row hidden>
                  <span>{{ section.settings.label_message }}</span>
                  <strong>{{ 'content.added' | t }}</strong>
                </div>

                <hr class="customize-box__rule">

                <div class="customize-box__summary-total">
                  <span>{{ section.settings.summary_total_label }}</span>
                  <strong data-summary-total>{{ 0 | money }}</strong>
                </div>

                {%- comment -%}
                  Horizon's add-to-cart-component is bound to a product form and a
                  single variant, neither of which exists here, so this is a plain
                  button the builder's own script drives. What is lost with it is
                  the flying-image animation; what is kept is every other part of
                  the cart, because the script raises the same event the component
                  would have raised.
                {%- endcomment -%}
                <div class="customize-box__add" hidden data-add-wrapper>
                  {% render 'ui-button',
                    variant: 'primary',
                    label: add_to_cart_label,
                    type: 'button',
                    full_width: true,
                    attributes: 'data-add-button'
                  %}
                  <p class="customize-box__add-error" hidden data-add-error></p>
                </div>
              </div>
            </aside>
          </div>
      </div>
    </customize-box>
  {%- endif -%}
</div>

<script src="{{ 'ui-customize-box.js' | asset_url }}" type="module"></script>

{% stylesheet %}
  .customize-box__hero {
    margin-block-start: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    box-sizing: border-box;
    padding: 32px 24px 48px;
    border-radius: 20px;
    background-color: var(--ui-color-light-blue);
    text-align: center;
  }

  .customize-box__eyebrow {
    display: flex;
    align-items: center;
    gap: 4px;
    margin: 0;
    font-family: var(--ui-font-accent);
    font-size: 12px;
    font-weight: var(--font-accent--weight-semibold, 600);
    line-height: 1.35;
    text-transform: uppercase;
    color: var(--ui-color-primary);
  }

  .customize-box__eyebrow-icon,
  .customize-box__eyebrow-icon > svg {
    display: block;
    inline-size: 16px;
    block-size: 16px;
  }

  .customize-box__heading {
    margin: 48px 0 16px;
    font-family: var(--font-heading--family);
    font-size: 48px;
    font-weight: 700;
    line-height: 1.1;
    color: var(--ui-color-black);
  }

  .customize-box__heading-accent {
    color: var(--ui-color-primary);
  }

  .customize-box__subheading {
    max-inline-size: 46ch;
    margin: 0;
    font-family: var(--ui-font-body);
    font-size: 16px;
    line-height: 1.3;
    color: var(--ui-color-gray);
  }

  /*
   * 710 and 522 with 48 between them is what the file draws at 1440, and 1280 is
   * the column they sit in — so the two are written as their own share of it and
   * hold their proportions wherever the column ends up.
   */
  .customize-box__layout {
    display: grid;
    gap: 48px;
    margin-block-start: 20px;

    /*
     * The indicator and the gap under it — the height of everything above the
     * first card. The file starts the summary level with that card and not with
     * the indicator over it, and starts the review card there too even though it
     * has no indicator of its own, so both are given the same lead.
     */
    --th-step-lead: 49px;
  }

  @media screen and (min-width: 1200px) {
    .customize-box__layout {
      grid-template-columns: minmax(0, 710fr) minmax(0, 522fr);
      align-items: start;
    }
  }

  .customize-box__main {
    display: flex;
    flex-direction: column;
    gap: 48px;
    min-inline-size: 0;
  }

  /*
   * 48 is the space between two step blocks, which only the first screen has —
   * it carries steps 1 and 2 together, the way the file draws them. Every other
   * screen is one indicator and one card, and that pair is 20 apart like the
   * pair inside a step block.
   */
  .customize-box__screen {
    display: flex;
    flex-direction: column;
    gap: 48px;
  }

  .customize-box__screen:not([data-screen='1']) {
    gap: 20px;
  }

  .customize-box__screen[hidden] {
    display: none;
  }

  .customize-box__step {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  /* Half the border gray, which is what the file draws: a line lighter than the
     one around the choices inside it, so the card reads as the ground and they
     read as the things on it. */
  .customize-box__card {
    display: flex;
    flex-direction: column;
    gap: 24px;
    box-sizing: border-box;
    padding: 24px;
    border: 1px solid rgb(var(--ui-color-border-gray-rgb) / 0.5);
  }

  .customize-box__card-head {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  /* A phone cannot spare 24 a side. Dropping to 16 gives the grid inside 32px
     back, which is the difference between two treats to a row and one. */
  @media screen and (max-width: 749px) {
    .customize-box__card {
      padding: 16px;
    }
  }


  .customize-box__card-title {
    margin: 0;
    font-family: var(--font-heading--family);
    font-size: 24px;
    font-weight: 700;
    line-height: 1.1;
    color: var(--ui-color-black);
  }

  .customize-box__card-number {
    margin-inline-end: 4px;
  }

  .customize-box__card-intro {
    margin: 0;
    font-family: var(--ui-font-body);
    font-size: 12px;
    line-height: 1.1;
    color: var(--ui-color-gray);
  }

  .customize-box__rule {
    inline-size: 100%;
    margin: 0;
    border: 0;
    border-block-start: 1px solid rgb(var(--ui-color-border-gray-rgb) / 0.5);
  }

  .customize-box__packs {
    display: flex;
    gap: 24px;
    align-items: stretch;
  }

  .customize-box__packs > .ui-choice-tile {
    block-size: 203px;
  }

  @media screen and (max-width: 749px) {
    .customize-box__packs {
      flex-direction: column;
      gap: 12px;
    }

    .customize-box__packs > .ui-choice-tile {
      block-size: auto;
    }
  }

  /*
   * Counted from the column, not the window. How wide this grid is depends on
   * whether the summary is beside it, so the same viewport gives it 662px on a
   * phone-shaped single column and 567 at 1203 where the summary has taken its
   * share — and a media query, which can only see the window, gave four columns
   * to both. At 567 that is a 120px card holding a name that wraps three times
   * and a stepper with 22px to spare.
   *
   * `max()` inside the track does the counting: the floor is a quarter of the
   * column, so a wide one lands on exactly the four the file draws and never
   * five, and 150px, so a narrow one drops to three and then two rather than
   * squeezing. 662 gives 4, 567 gives 3, a phone gives 2.
   */
  .customize-box__flavours {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(max(140px, calc((100% - 36px) / 4)), 1fr));
    gap: 12px;
  }

  .customize-box__tally {
    margin: 0;
    font-family: var(--ui-font-body);
    font-size: 12px;
    line-height: 1.3;
    color: var(--ui-color-gray);
  }

  .customize-box__tally[data-complete] {
    color: var(--ui-color-primary);
  }

  .customize-box__packaging {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .customize-box__review {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .customize-box__actions {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }

  .customize-box__promo {
    display: flex;
    flex-direction: column;
    gap: 6px;
    box-sizing: border-box;
    padding: 14px;
    border-radius: var(--ui-radius-sm);
    background-color: var(--ui-color-light-blue);
  }

  .customize-box__promo-title {
    display: flex;
    align-items: center;
    gap: 4px;
    margin: 0;
    font-family: var(--ui-font-accent);
    font-size: 12px;
    font-weight: var(--font-accent--weight-semibold, 600);
    line-height: 1.35;
    text-transform: uppercase;
    color: var(--ui-color-primary);
  }

  .customize-box__promo-icon,
  .customize-box__promo-icon > svg {
    display: block;
    inline-size: 16px;
    block-size: 16px;
  }

  .customize-box__promo-text {
    display: flex;
    flex-direction: column;
  }

  .customize-box__promo-copy {
    margin: 0;
    max-inline-size: 62ch;
    font-family: var(--ui-font-body);
    font-size: 14px;
    line-height: 1.3;
    color: var(--ui-color-black);
  }

  @media screen and (min-width: 750px) {
    .customize-box__promo {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      column-gap: 24px;
    }

    .customize-box__promo-title {
      grid-column: 1 / -1;
    }
  }

  .customize-box__summary-inner {
    display: flex;
    flex-direction: column;
    gap: 24px;
    box-sizing: border-box;
    padding: 20px;
    border: 1px solid rgb(var(--ui-color-border-gray-rgb) / 0.5);
  }

  @media screen and (min-width: 1200px) {
    .customize-box__summary {
      margin-block-start: var(--th-step-lead);
      position: sticky;
      inset-block-start: calc(var(--header-height, 0px) + 24px);
    }

    /* The one screen with no indicator keeps the space where one would be, so
       its card starts level with the summary like every other screen's does. */
    .customize-box__screen[data-screen='4'] {
      margin-block-start: var(--th-step-lead);
    }
  }

  .customize-box__summary-title {
    margin: 0;
    font-family: var(--font-heading--family);
    font-size: 24px;
    font-weight: 700;
    line-height: 1.1;
    color: var(--ui-color-black);
  }

  .customize-box__summary-row,
  .customize-box__summary-total {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 16px;
    font-family: var(--ui-font-body);
    font-size: 16px;
    line-height: 1.1;
    color: var(--color-foreground-heading);
  }

  .customize-box__add-error {
    margin-block-start: 8px;
    font-family: var(--ui-font-body);
    font-size: 12px;
    line-height: 1.3;
    color: var(--ui-color-error);
  }

  .customize-box__summary-row > span {
    font-size: 14px;
    line-height: 1.3;
    color: var(--ui-color-gray);
  }

  /* Regular where the choice beside it is bold, and a size down. */
  .customize-box__summary-extra {
    font-weight: var(--ui-font-body-weight);
    font-size: 14px;
    line-height: 1.3;
  }

  .customize-box__summary-total > span {
    font-family: var(--ui-font-accent);
    font-size: 12px;
    font-weight: var(--font-accent--weight-semibold, 600);
    text-transform: uppercase;
  }

  .customize-box__summary-total > strong {
    font-size: 24px;
    line-height: 1.1;
  }

  /* Two to a row across the 482 the summary has inside its padding, which is
     what makes each 236 wide — and one to a row once that column is narrow
     enough that two would leave no room beside a 46px picture. Counted the same
     way as the treats above, and for the same reason. */
  .customize-box__summary-chips {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(max(160px, calc((100% - 10px) / 2)), 1fr));
    gap: 10px;
  }

  /*
   * The picture is a block of the chip, not something sitting inside it: 46px
   * square against the left edge with a single pixel of the chip showing round
   * it, and the words carrying their own padding. That is why the chip's own
   * padding is 1px and the text block's is 6 and 10 — the two are different
   * boxes, and giving the chip the text's padding would push the picture off
   * the edge it belongs to.
   */
  .customize-box__chip {
    display: flex;
    align-items: center;
    box-sizing: border-box;
    min-block-size: 48px;

    /* No padding of its own. The file insets the picture by 1px from the frame
       edge and draws the stroke inside that same pixel, so in CSS the border is
       the inset — adding a padding on top of it makes the chip 50. */
    padding: 0;
    border: 1px solid rgb(var(--ui-color-border-gray-rgb) / 0.5);
    border-radius: var(--ui-radius-sm);
    background-color: var(--ui-color-white);
    font-family: var(--ui-font-body);
    font-size: 12px;
    line-height: 1.1;
    color: var(--ui-color-black);
  }

  /* Both sides written out. An image is a replaced element: given a width and
     left to work its height out, it takes the photograph's proportions and
     `align-self: stretch` never gets a say — which made the chip as tall as
     whatever was photographed rather than the 48 it is drawn at. */
  .customize-box__chip-image {
    display: block;
    flex: 0 0 auto;
    inline-size: 46px;
    block-size: 46px;
    border-radius: 1px;
    object-fit: cover;
  }

  .customize-box__chip-body {
    display: flex;
    flex: 1 1 auto;
    align-items: center;
    gap: 8px;
    min-inline-size: 0;
    padding: 6px 10px;
  }

  .customize-box__chip-name {
    min-inline-size: 0;
  }

  .customize-box__chip-count {
    margin-inline-start: auto;
    font-weight: 700;
  }

  .customize-box__setup {
    margin: 24px 0 0;
    font-family: var(--ui-font-body);
    font-size: 14px;
    line-height: 1.3;
    color: var(--ui-color-gray);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Customize your box",
  "tag": "section",
  "settings": [
    {
      "type": "paragraph",
      "content": "The pack sizes are products and the packaging is their own options, so the price always comes from Shopify. Flavours and the gift message ride along as line item properties."
    },
    {
      "type": "collection",
      "id": "pack_collection",
      "label": "Pack sizes",
      "info": "One product per size. The size is read from the product's custom.pack_size metafield, or from the number its title starts with."
    },
    {
      "type": "collection",
      "id": "flavour_collection",
      "label": "Flavours collection"
    },
    {
      "type": "product_list",
      "id": "flavour_products",
      "label": "Flavours",
      "limit": 24,
      "info": "Used instead of the collection above when it has anything in it."
    },
    {
      "type": "text",
      "id": "unit_noun",
      "label": "Unit name",
      "default": "treat",
      "info": "Used for the per-unit price under each pack: “$3.00 / treat”."
    },
    {
      "type": "header",
      "content": "Breadcrumb"
    },
    {
      "type": "text",
      "id": "breadcrumb_parent_title",
      "label": "Parent page",
      "default": "Gift Boxes"
    },
    {
      "type": "url",
      "id": "breadcrumb_parent_url",
      "label": "t:settings.link"
    },
    {
      "type": "header",
      "content": "t:content.heading"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Customize"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:settings.heading",
      "default": "Design"
    },
    {
      "type": "text",
      "id": "heading_accent",
      "label": "Heading in purple",
      "default": "your box"
    },
    {
      "type": "text",
      "id": "heading_after",
      "label": "Heading after step one",
      "default": "Your box,",
      "info": "The title changes once the reader is past the first step. Leave either field empty to keep the wording above for the whole builder."
    },
    {
      "type": "text",
      "id": "heading_accent_after",
      "label": "Heading in purple after step one",
      "default": "your way"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "t:settings.text",
      "default": "Pick your flavors, choose the packaging, add a note, and review. Four short steps, and you can go back at any point."
    },
    {
      "type": "header",
      "content": "Steps"
    },
    {
      "type": "text",
      "id": "step_1_title",
      "label": "Step 1",
      "default": "Choose your quantity"
    },
    {
      "type": "text",
      "id": "step_1_intro",
      "label": "Step 1 note",
      "default": "The price per treat comes down as the box grows."
    },
    {
      "type": "text",
      "id": "step_2_title",
      "label": "Step 2",
      "default": "Pick your flavors"
    },
    {
      "type": "text",
      "id": "step_2_intro",
      "label": "Step 2 note",
      "default": "Tap a treat to add it, then set how many of each you would like."
    },
    {
      "type": "text",
      "id": "step_3_title",
      "label": "Step 3",
      "default": "Choose the packaging"
    },
    {
      "type": "text",
      "id": "step_3_intro",
      "label": "Step 3 note",
      "default": "How your treats will arrive, packed with care."
    },
    {
      "type": "text",
      "id": "step_4_title",
      "label": "Step 4",
      "default": "Add a gift message"
    },
    {
      "type": "text",
      "id": "step_4_intro",
      "label": "Step 4 note",
      "default": "Optional. Printed on our signature card and tucked inside the box."
    },
    {
      "type": "text",
      "id": "message_placeholder",
      "label": "Message placeholder",
      "default": "Write a few words for them"
    },
    {
      "type": "range",
      "id": "message_maxlength",
      "label": "Message length",
      "min": 50,
      "max": 500,
      "step": 10,
      "default": 200
    },
    {
      "type": "header",
      "content": "Review"
    },
    {
      "type": "text",
      "id": "review_title",
      "label": "t:settings.title",
      "default": "Review your box"
    },
    {
      "type": "text",
      "id": "review_intro",
      "label": "t:settings.text",
      "default": "Take a last look, then add it to your cart below."
    },
    {
      "type": "header",
      "content": "Summary"
    },
    {
      "type": "text",
      "id": "summary_title",
      "label": "t:settings.title",
      "default": "Your box"
    },
    {
      "type": "text",
      "id": "label_flavours",
      "label": "Flavours",
      "default": "Flavors"
    },
    {
      "type": "text",
      "id": "label_flavours_selected",
      "label": "Flavours chosen",
      "default": "Flavors selected"
    },
    {
      "type": "text",
      "id": "label_quantity",
      "label": "Quantity",
      "default": "Quantity"
    },
    {
      "type": "text",
      "id": "label_packaging",
      "label": "Packaging",
      "default": "Packaging"
    },
    {
      "type": "text",
      "id": "label_message",
      "label": "Gift message",
      "default": "Gift message"
    },
    {
      "type": "text",
      "id": "summary_total_label",
      "label": "Total row",
      "default": "Total"
    },
    {
      "type": "header",
      "content": "Corporate branding note"
    },
    {
      "type": "text",
      "id": "promo_title",
      "label": "t:settings.title",
      "default": "Your brand, made personal"
    },
    {
      "type": "textarea",
      "id": "promo_text",
      "label": "t:settings.text",
      "default": "Your logo and colors on every treat, for clients, teams, and events. Send us a note and a real person will follow up with pricing and a proof."
    },
    {
      "type": "text",
      "id": "promo_button_label",
      "label": "t:settings.label",
      "default": "Plan a thoughtful client gift"
    },
    {
      "type": "url",
      "id": "promo_button_link",
      "label": "t:settings.link"
    },
    {
      "type": "header",
      "content": "t:content.layout"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top_padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom_padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "_ui-box-packaging"
    }
  ],
  "presets": [
    {
      "name": "Customize your box",
      "blocks": [
        {
          "type": "_ui-box-packaging",
          "settings": {
            "title": "Clear Tray",
            "description": "Simple and clear, for everyday sharing",
            "gift_box": false,
            "individual_packs": false
          }
        },
        {
          "type": "_ui-box-packaging",
          "settings": {
            "title": "Gift Box",
            "description": "Our signature box, gift-ready on arrival",
            "gift_box": true,
            "individual_packs": false
          }
        },
        {
          "type": "_ui-box-packaging",
          "settings": {
            "title": "Individually Wrapped",
            "description": "Wrapped one by one, for favors and sharing",
            "gift_box": false,
            "individual_packs": true
          }
        }
      ]
    }
  ]
}
{% endschema %}
